from utils.test_llm_responses import run
# from sqlalchemy import create_engine
# from infrastructure.config import settings

if __name__ == "__main__":
    run()

    # engine = create_engine(
    #     url=settings.DB_URL,
    #     echo=True,
    # )
    # print(settings.DB_URL)
